{"ts": "2026-08-16T15:56:22Z", "author": "agent", "text": "DEFECT, measured 2026-08-16: this uses builtin hash() on token tuples, which Python randomizes per process (PYTHONHASHSEED). Sketches are PERSISTED in coordinates.json and parse_cache.json, so values computed by different processes coexist and are compared to each other.\n\nTwo consequences, both reproduced. (1) Indexing the same tree in two fresh clones produces byte-identical output EXCEPT `sketch`, which differs on 888/888 entities - so `git diff --exit-code .coord` can never be a CI staleness gate, and every parallel index conflicts in coordinates.json. (2) sketch_jaccard is the largest single term in lineage's rename score (weight 0.30) and sketch_containment is the ONLY signal behind split/merge detection; across process boundaries - i.e. every real rename, since the old sketch comes off disk and the new one is computed now - both are noise. Isolated experiment, identical repo and commits, only PYTHONHASHSEED varied: default gave `lineage: deleted m.compute_totals` (no relationship recorded, attached knowledge orphaned with no trail); PYTHONHASHSEED=7 on both runs gave `merged -> m.ledger_rollup - needs confirmation` (recorded, pending-review, recoverable). Randomization converts \"flagged for a human\" into \"silently deleted\".\n\nFix is a stable hash (e.g. blake2b of the joined tokens), but note the migration: existing persisted sketches are random values, so the change must bump the parse schema version and force a full recompute in ONE pass, or old-vs-new comparisons will read as zero similarity for exactly one transition - the same failure this note describes.", "body_hash": "e1b704a05e4883f0"}
{"ts": "2026-08-16T17:07:52Z", "author": "agent", "text": "SUPERSEDES the defect note below it (2026-08-16): that defect is FIXED as of 0.54.0 and the note now describes history, not this code. Read it for the reasoning, not for what the function does.\n\nWhat is true now: the shingle hash is blake2b at digest_size=6 - exactly the 48 bits the permutation masks to - so sketches are STABLE ACROSS PROCESSES. Two fresh clones of one sha produce byte-identical .coord, verified on the published wheel against itsdangerous and flask.\n\nWhat did NOT change: the algorithm, the shingle width (token 3-grams), _SKETCH_K=48, and the permutation. Only the shingle hash. Jaccard and containment semantics are identical provided both sides came from the same SKETCH_VERSION - which is why SKETCH_VERSION exists and why lineage refuses to use the signal across a generation boundary.\n\nDo not \"simplify\" this back to builtin hash(), and do not add Path.resolve()-style tidying anywhere near the version stamping. The persisted-value trap is the whole point: anything written into coordinates.json must be reproducible by a different process on a different machine.", "body_hash": "91ec3ce4a2cea270"}
