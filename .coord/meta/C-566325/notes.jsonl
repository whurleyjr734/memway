{"ts": "2026-08-23T06:56:01Z", "author": "agent", "text": "MEASURED ON REAL REPOS 2026-08-22, immediately after shipping. Two things worth knowing before changing this.\n\nTHE PAYLOAD BUG, and it is the reason MEMBERS_CAP exists. The first version bounded GROUPS at twelve and let each group carry every member. On pytest@df87db7 that returned 12 groups holding 193 member rows - 36,433 characters, roughly 9,000 tokens for one call - and nothing in the payload said a list had been cut, because none had. The rule was applied at the top level and forgotten one level down. payload.py's docstring names exactly this shape and this is the seventh instance of it in this codebase. Bounding both levels took it to 12,424 characters, a 66 percent cut, with members_shown and members_total on every group.\n\nWHAT THE RESULTS ACTUALLY LOOK LIKE ON REAL CODE, so nobody mistakes it for a defect. The top groups on pytest and pydantic are dominated by TEST entities - 27 structurally identical test bodies, 20 three-line validators inside one dataclass test. That is true duplication and the hash is right, but it is not what a reader means by \"where does my repo repeat itself\". On memway's own map the largest group is the genuine finding: one 3-line subprocess helper copied into seven test files under two names, which is worth fixing.\n\nSo the signal is honest and the RANKING is by group size, which on a test-heavy repo puts parametrized tests first. If this is ever tuned, the fix is a production/test split reported as a fact - is_test_entity already exists and is the one rule for it - not a filter that silently drops entities, and not a floor raised until the noise disappears. Raising min_loc was tried during design and discards the true positives before the noise: at loc>=5 memway's own _cli and _git groups vanish and only 3 groups survive.\n\nTHE NEAR TIER IS CORRECT AND SLOW-LOOKING BUT IS NOT. It is O(n) sketch comparisons against every eligible entity - 6,699 on pydantic - and completes in 0.4s because the sketch is 48 packed integers. Verified end to end rather than assumed: model_dump's best neighbour is model_dump_json at 0.646, which is semantically the right answer, and a threshold of 0.8 correctly returns nothing. A zero from this tier is a real zero, not a silent failure - checked by confirming the sketch was populated (48 values, 483 shingles) before believing the empty result.", "body_hash": "2ee2eb28ec0564ca"}
{"ts": "2026-08-23T16:47:10Z", "author": "agent", "text": "TUNED IN 0.63.2, exactly as the note below prescribed, and that note is now history - read it for the measurements, not for what this code does.\n\nWHAT SHIPPED. Each group carries an `origin` - production, mixed, or test-only - and the payload carries `groups_by_origin` as a census. Ranking is origin first, then size. That is all: the totals still count every group, no entity is dropped, and only the ORDER changed.\n\nWHY THE RANKING WAS THE BUG AND NOT THE SIGNAL. 351 of pydantic's 471 duplicated groups are test-only, so ranking by size handed the entire visible page to parametrized test bodies. What it never surfaced: ten identical six-line property accessors across _BaseUrl and _BaseMultiHostUrl in pydantic/networks.py - production code, and exactly what somebody runs this tool to find. It is now second on the page. On memway's own map the split immediately surfaced three identical parser constructors (GoParser, JavaParser, JavaScriptParser.__init__) that twelve test groups had been sitting on top of.\n\nBOTH REJECTED ALTERNATIVES ARE STILL REJECTED, and the reasons are measured rather than argued. Filtering tests out discards real duplication silently - memway's own largest finding is a test helper copied into seven files under two names, and it is worth fixing. Raising min_loc kills the true positives FIRST: at loc>=5, 3 of 14 groups survive here and both _cli and _git vanish.\n\nMIXED IS SMALLER THAN THE IDEA SUGGESTS and that is recorded on _origin rather than hidden: 4 groups of 471 on pydantic, and at three lines they look coincidental. It is a real category - a test reimplementing production logic instead of calling it - carrying little weight on this data. Anyone building on it should check the size first.\n\nTHE CLASSIFICATION IS ABOUT LOCATION, NOT INTENT. is_test_entity decides by path, which is why a helper under tests/ counts as a test and a vendored subtree like pydantic-core counts as production. That is a judgement the reader may disagree with, which is the argument for the census: it is a fact they can weigh, not a filter they cannot see.", "body_hash": "96535f71886e0c29", "replaces": "the note prescribed a production/test split as the fix IF the ranking were ever tuned; it has now been tuned exactly that way, so the conditional is spent"}
