{"ts": "2026-08-08T19:56:22Z", "author": "cli", "text": "Defect in comment_rot detection (lines 555-563): logic is delta-based, not consistency-based. Sets rot=True whenever prev_logic_hash != current_logic_hash, which cannot distinguish \"logic changed away from comments\" from \"logic changed back to match comments.\" Once set, the flag persists even when code reverts to a state where comments accurately describe the logic. No automatic path back to False except by changing comment_hash, which is wrong when comments are already correct. Separately: confirm suppresses the rot DISPLAY in attention but does not clear the flag in the index. Needs consistency checking, not just delta detection.", "body_hash": "d6790925f26b0f08"}
{"ts": "2026-08-16T02:14:41Z", "author": "cli", "text": "CORRECTION to the earlier comment_rot note on this coordinate (2026-08-08, author cli), which claimed 'No automatic path back to False'. That is not what the code does. Verified against indexer.py:559-567: if prev_ch != comment_hash the flag is CLEARED (line 563), so editing the comment does reset it. The three branches are: comments changed -> False; comments unchanged AND logic_hash moved -> True; neither -> inherit the previous value. So comment_rot is a DELTA-BASED LATCH, not a consistency check, and the sticky third branch is the real defect surface: fixing the CODE so the comment becomes accurate again leaves the flag True forever, because only a comment edit clears it. The original note's substance (delta vs consistency, no way back from a code-side fix) stands; its absolute claim does not. Read a rot flag as 'logic moved at some point without the comment moving', never as 'this comment is wrong right now'.", "body_hash": "2214084736a00110"}
