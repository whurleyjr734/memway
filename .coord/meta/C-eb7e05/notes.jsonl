{"ts": "2026-08-17T01:11:46Z", "author": "cli", "text": "RULE 1 IS TOO TIGHT, and its own docstring says so. The prose is right - 'a class or def inside a function body cannot be named from OUTSIDE that body' - but the code drops the target unconditionally: it walks dst_ent's parents, finds a function, and returns 'function-local' without ever asking where src_ent is. When the caller IS the enclosing function, the target is perfectly reachable, and that is the ordinary Python closure idiom. MEASURED 2026-08-16 under 0.55.2 against three real Python repos outside the corpus (psf/requests 8068356, pallets/click cbd7a41, Textualize/rich 9d8f9a3), ground truth from stdlib ast with real scope tracking: 60 true call edges dropped where the target is a def nested DIRECTLY inside the caller. rich alone loses 50, including rich.tree.Tree.__rich_console__ -> make_guide, which is its core render path and is called six times in that one function. Overall Python edge recall: requests 97%, rich 90%, click 68%. FIX: before returning 'function-local', check whether src_ent is the enclosing function or nested within it - if so the target is in scope and the edge is real. Cheap and unambiguous; a call from a function to a def it directly contains has exactly one candidate. DO NOT loosen rules 2 and 3 with it: the attribute-call rule is doing real work - 111 of the 184 misses in this measurement are receiver.method() calls that memway REFUSES to guess, which is correct behaviour and the whole point of the 23% fix. The oracle counts those as misses; they are not. This is the 'unfalsified in the too-tight direction' risk that has been on the open list since 0.54.3, now with a number and a reproduction.", "body_hash": "6ac3017c39d30bb3"}
{"ts": "2026-08-17T01:26:40Z", "author": "cli", "text": "FIXED in 0.55.3 - supersedes the note below, which diagnosed it. Rule 1 now asks _in_scope(src_ent, encloser) before refusing: if the caller IS the enclosing function, or is nested within it, the target is lexically in scope and the edge stands. The check walks UP from the caller, so a sibling closure calling its parent's helper resolves too. Re-measured on the same three repos at the same shas: closure misses 60 -> 3 (requests 2->1, click 8->2, rich 50->0), and rich's overall recall 90% -> 98% with Tree.__rich_console__ -> make_guide now present. Rules 2 and 3 were NOT touched, as the diagnosis insisted: the 111 attribute-call refusals are the 0.54.3 fix working, and loosening them would trade a small under-report for the 369-edge hairball. THE REMAINING 3 ARE A DIFFERENT DEFECT, filed not fixed: where a def has @overload stubs, the parser emits src_qualname WITHOUT the #N disambiguator (raw edge reads 'src.requests.models.Response.iter_content'), so the call is attributed to the first stub while the nested helper is parented to iter_content#3. The scope check then correctly says the stub does not contain it. Same shape as the Java arity suffix: one name, two representations, emitter and registry unable to meet. PINNED IN TWO LAYERS because one was demonstrably not enough - every fixture in the suite passed while this bug shipped, since none had the caller be the encloser. Fast fixture in test_units_core2 (falsified both ways: revert the scope check, and remove guard 1 entirely), corpus floors in test_corpus_recall under 'pytest -m network'.", "body_hash": "ce3fe69b06afb02c"}
{"ts": "2026-08-19T06:23:47Z", "author": "agent", "text": "DEFECT, found by a human looking at the rendered map on 2026-08-18 and measured on prometheus/prometheus@6063ce7. A LANGUAGE BUILTIN IS BEING RESOLVED TO A REPO METHOD, at full confidence.\n\ntsdb/head.go declares `func (c *memChunk) len() int`. It is the only entity in the repo whose short name is `len`, so candidates(\"len\") returns exactly one, resolution is \"exact\", and EVERY `len(x)` in prometheus lands on it:\n\n  tsdb.head.memChunk.len          1,619 incoming call edges\n  next highest in the whole map     342\n\n1,619 of 16,346 call edges - 9.9% of the map's call traffic - point at one 3-line method it has nothing to do with. Uniqueness was read as certainty, which is the same mistake 0.54.3 fixed for ambiguous names, arriving from the other direction: no competition looked like confidence when it is the case that most deserves suspicion.\n\nTHE RULE THAT WOULD FIX IT is already here in mirror image. Rule 2 says an ATTRIBUTE call cannot be a module-level function - `d.get(x)` is not `def get`. The converse is just as true: a BARE call cannot be a METHOD. `len(x)` is written with no receiver, and `memChunk.len` cannot be reached without one, however unique the name.\n\nDO NOT IMPLEMENT IT YET, and this is the load-bearing part. via_attr is populated by the PYTHON PARSER ONLY (parsers.py, PythonParser). GoParser, JavaParser and the JS/TS parsers never set it, so every one of their calls arrives via_attr=False. A rule reading \"bare call, therefore not a method\" would treat EVERY Go and Java call as bare and refuse every method target - deleting the 3,150 gson method-to-method edges 0.56.0 existed to create. The rule is right and the data is not there to support it in four of five languages.\n\nSo the honest order is: teach the tree-sitter parsers to record via_attr (they already walk the call node and can see whether the function expression is a selector/field access), pin that each parser sets it, and only then add the mirror rule. Doing the rule first turns a 10% false-edge problem into a total loss of polyglot method resolution.\n\nSCOPE. This is not only builtins. The same shape appears below `len` in the same map: DirLocker.Lock takes 342 edges, and `mu.Lock()` on a stdlib sync.Mutex is the obvious source; Timer.Duration takes 211. The general class is a call whose real target is OUTSIDE the repo - builtin, stdlib, or dependency - being attributed to a same-named entity inside it. Builtins are the tractable head of that distribution because the set is finite and defined by the language spec.\n\nNot fixed as of 0.57.2. Recorded here because the reasoning for NOT fixing it in the obvious way is the part that would be lost.", "body_hash": "ce3fe69b06afb02c"}
{"ts": "2026-08-19T15:47:07Z", "author": "agent", "text": "SUPERSEDES the DEFECT note below. Fixed in 0.57.2; read that note for the measurements, not for what this code does.\n\nWHAT SHIPPED, and it is narrower than the note below proposed. Rule 2b refuses a call only when the callee is a METHOD, the call site wrote NO receiver, and the name is one the CALLER'S LANGUAGE defines. prometheus: memChunk.len went 1,619 incoming call edges to 13, and the 13 are real - `chk.len()` with a receiver, one of which sits on the same line as a builtin call (`removedInOrder = chk.len() + len(s.mmappedChunks)`), which is now the Go fixture. gson holds at exactly 3,150 method-to-method edges, the number 0.56.0 built.\n\nTHE BROAD RULE THE NOTE BELOW PROPOSED IS FALSE, and this is the part worth keeping. \"A bare call cannot be a method\" is not true of Python: rich binds `add_row = items_table.add_row` and then calls `add_row(...)`. A bound-method alias keeps the name, so the reference really is that method, reached with no receiver. JS destructuring does the same. Implemented as stated, the rule cost rich 96% -> 94% and sqlalchemy its floor, in the first network run after the change. The corpus floors caught it; no fixture did, and no fixture would have - this is CLAUDE.md lesson 5 paying for itself.\n\nRestricted to names the language itself defines, the claim is true again and still covers all of the measured harm, because the harm was builtins colliding with same-named methods.\n\nTWO PIECES OF DATA HAD TO EXIST FIRST, both easy to get wrong:\n\n1. via_attr was populated by the PYTHON PARSER ALONE. Go, Java and JS/TS never set it, so their calls all arrived looking receiverless. Adding any bare-call rule before fixing that would have refused every polyglot method target.\n\n2. via_attr CANNOT ANSWER \"was a receiver written\". Python clears it when dst_override resolves an imported receiver - via_attr means \"attribute call the parser could not resolve to a module\". Reusing it as the bare-call signal refused every resolved-receiver call to a method. RawEdge.bare is a separate field for exactly this reason, and the two must not be merged again.\n\nPARSE_SCHEMA_VERSION went 7 -> 10 across this work, one bump per field or semantic change: 8 for via_attr on the tree-sitter parsers, 9 for RawEdge.bare, 10 for builtin_names and the narrowed rule. Each was necessary - a cache warmed at the previous version replays edges missing the new field, which silently disables the rule rather than failing.\n\nSTILL OPEN, and unchanged by this: a call whose real target is outside the repo but is NOT a builtin. DirLocker.Lock still takes 342 edges on prometheus (`mu.Lock()` on a stdlib sync.Mutex) and Timer.Duration 211. Builtins were the tractable head of that distribution because the set is finite and the language defines it; stdlib and dependency receivers need type information the map does not have.", "body_hash": "ba6ce81978bf4248"}
{"ts": "2026-08-20T04:33:29Z", "author": "agent", "text": "THE LITERAL-RECEIVER CASE, measured on scikit-learn@1be3c64 (1,019 files, 450k lines) 2026-08-20. This is the tractable head of the stdlib-receiver problem the note above leaves open, and it is narrower and more certain than that note implies.\n\nsklearn.utils._pprint._EstimatorPrettyPrinter.format takes 292 incoming call edges at resolution \"exact\", confidence 0.95. The callers are lines like:\n\n  print(\"score: {:.4f}\".format(sklearn_score))          bench_hist_gradient_boosting.py:130\n\nThat is str.format on a STRING LITERAL. The file defines no `format` and imports none. It is the single most common formatting idiom in Python, and it lands on a pretty-printer method it has nothing to do with - seventh-highest in-degree in the whole map.\n\nWHY 0.57.2's RULE DOES NOT CATCH IT, and this is the useful part. That rule needs bare=True (no receiver written). `\"...\".format(x)` HAS a receiver, so bare is False and the rule correctly declines to fire. The builtin-name check is about receiverless calls; this is a receiver whose TYPE is known.\n\nWHAT MAKES IT DIFFERENT FROM THE REST OF THE STDLIB PROBLEM: no type inference is required. The AST states the receiver's type outright. `\"...\"` is a str, `[...]` is a list, `{...}` is a dict. mu.Lock() needs to know what mu is; \"x\".format() does not.\n\nMEASURED SCOPE on sklearn: 60,635 attribute calls, of which 619 (1%) have a literal receiver - .format 426, .join 159, then .ljust, .split, .center, .encode, .strip in single digits. Small as a fraction, concentrated in exactly the names a repo is likely to reuse: format and join are ordinary method names.\n\nPROPOSED RULE, not implemented: if the call site's receiver is an ast.Constant, JoinedStr, List, Dict, Set, Tuple or a comprehension, the target is a builtin type's method and no repo entity may claim it. Python only - the tree-sitter parsers would need the same fact recorded, and RawEdge would need to carry it the way `bare` is carried, which per 0.57.2 means a PARSE_SCHEMA_VERSION bump.\n\nThe falsification to write first: a fixture with a class method named `format` and a caller doing \"{}\".format(x), asserting no edge forms - and a SECOND caller doing obj.format(x) where obj is a repo instance, asserting that edge DOES form. One without the other passes for the wrong reason.\n\nFor the record from the same run: sklearn indexes to 14,347 entities and 46,037 edges in 65s cold, 20s incremental with 0 recomputed; 87% recall against the builtin-aware oracle (4,523/5,181) with 0 closure misses; 1,151 inherits edges and 261 MRO-resolved calls, the deepest inheritance this project has been run against.", "body_hash": "ba6ce81978bf4248"}
{"ts": "2026-08-20T04:50:34Z", "author": "cli", "text": "IMPLEMENTED. Supersedes the proposal in the note below; that note has the measurements, this one has what shipped.\n\nRule 2c: if the call site's receiver was written as a LITERAL, no repo entity may claim the call. `\"{}\".format(x)` is str.format. The syntax states the receiver's type, so unlike the rest of the stdlib-receiver problem this needs no inference.\n\nscikit-learn@1be3c64: _EstimatorPrettyPrinter.format went 292 incoming call edges to 46, and the 46 are real (self.format and printer.format on instances). Total edges 46,037 -> 45,789.\n\nRawEdge.literal_recv is a THIRD field, not an overload. The confirm on visit_Call records what merging `bare` and `via_attr` cost; the same argument applies here and the fields answer three different questions: no receiver / an attribute call we could not qualify / a receiver whose type is written down.\n\nRecorded by the Python, JS/TS and Java parsers. Go is deliberately absent: its literals have no methods, so the construct does not parse.\n\nTHE ORACLE NEEDED THE SAME FACT, for the second time. sklearn's measured recall 'fell' 87% -> 83% when this landed, and all 206 lost hits had a literal receiver - measured, not assumed, because the identical symptom would appear if the rule were deleting real edges. tests/test_corpus_recall now reads the parser's own predicate rather than its own idea of a literal, so the two cannot disagree.\n\nSTILL OPEN and untouched: receivers whose type needs inference. On sklearn that is 662 further misses, led by np.finfo (89), np.where (88), np.empty_like (48) - a dependency's methods, not the language's.", "body_hash": "2c30ec26157261d8"}
{"ts": "2026-08-23T05:53:18Z", "author": "agent", "text": "MEASURED ON PYDANTIC @2151025, 2026-08-22 - the \"still open\" tail of the uniqueness-as-certainty family, sized on a repo none of the existing rules were tuned against. 20,687 entities, 12,180 call edges.\n\n2,040 call edges (16.7%) come from an attribute call whose receiver type was NEVER established, resolve to a single short-name match, and ship as resolution \"exact\", confidence 0.95.\n\nSpecimens, verified by reading the call sites rather than inferred:\n\n  keys -> pydantic.v1.utils.GetterDict.keys, 118 edges. The call sites\n  are `kwargs.keys()` and `config_dict.keys()` in\n  pydantic/_internal/_config.py. Plain dicts.\n\n  validate_python -> pydantic.type_adapter.TypeAdapter.validate_python,\n  1,119 incoming, of which 752 are inside the vendored pydantic-core\n  subtree. Those call sites read\n  `SchemaValidator(schema_using_defs()).validate_python(...)` - a\n  Rust-backed class with no Python definition in the map at all. The\n  name has exactly two definitions (this one and a conftest test\n  helper); rule 3 strips the test helper, the survivor is unique, and\n  uniqueness is read as certainty. It is the single highest fan-in\n  entity in the repo and its WIDELY DEPENDED ON warning carries no\n  caveat.\n\nWHAT IS NEW HERE IS NOT THE DEFECT, IT IS THAT HALF OF IT NEEDS NO TYPE INFORMATION. The note below frames the remainder as blocked on type inference - true for choosing the RIGHT target, false for declining to claim certainty about the wrong one. The original defect note on this family already prescribed the tractable half: \"a bare-name fallback must not score exact. Either drop to the low-confidence tier (<0.7, which the grounding block already surfaces and warns about)\".\n\nThat half is still undone for ONE shape: `via_attr` with no dot in dst_ref - an attribute call the parser could not qualify, matched purely by method name. It is distinct from every tier already handled: not `bare` (a receiver IS written, so rule 2b declines), not `literal_recv` (the receiver is an expression, not a literal, so 2c declines), not self/annotated/MRO (those all carry a dot). It falls through to the 0.95 default.\n\nTHE CONSEQUENCE IS SPECIFICALLY THAT THE WARNING BUILT FOR THIS IS BLIND TO IT. direct_callers_guessed and low_confidence_edges both key on confidence < LOW_CONFIDENCE, so these 2,040 edges report guessed: 0. The django ListMixin.append work added that caveat precisely so a 573-caller warning could say \"all of them are guesses\"; here a 1,119-caller warning says nothing, because the edges claim to be exact.\n\nPROPOSED, NOT DONE: give this shape its own tier at 0.60 - the edge stays (recall is preserved, and dropping it would hide real coupling) and only the label changes. Not done in this session because it moves 16.7% of a real map from exact to guess and the corpus floors are the ruler that would measure it. THE ORACLE TRAP APPLIES DIRECTLY: tests/test_corpus_recall.py shares the tool's blind spot twice before in this exact family, and a floor that moves after this change must be investigated before it is adjusted, never adjusted to make the run green.", "body_hash": "2c30ec26157261d8"}
